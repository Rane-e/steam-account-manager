services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: steam-account-manager/backend:latest
    env_file: backend/.env
    ports:
      - "5000:3000"
    restart: unless-stopped
